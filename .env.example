# =============================================================================
# AWS Configuration - Configuração AWS
# =============================================================================
# Este arquivo contém as variáveis de ambiente necessárias para o MCP da AWS
# Copie este arquivo para .env e preencha com suas credenciais

# AWS Profile - Nome do perfil AWS configurado localmente
# Se você não tem um perfil, use 'default'
AWS_PROFILE=default

# AWS Region - Região AWS onde seus recursos estão/serão criados
# Regiões comuns:
#   - us-east-1 (Norte da Virgínia)
#   - us-west-2 (Oregon)
#   - sa-east-1 (São Paulo)
#   - eu-west-1 (Irlanda)
AWS_REGION=us-east-1

# =============================================================================
# AWS Credentials (Opcional - use apenas se não usar AWS Profile)
# =============================================================================
# Se você não tem AWS CLI configurado, pode usar variáveis de ambiente
# ATENÇÃO: Nunca commite este arquivo com credenciais reais!

# AWS Access Key ID
# AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE

# AWS Secret Access Key
# AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# AWS Session Token (apenas se usar credenciais temporárias/STS)
# AWS_SESSION_TOKEN=your_session_token_here

# =============================================================================
# MCP Configuration - Configuração do MCP
# =============================================================================

# Nível de log do MCP (ERROR, WARN, INFO, DEBUG)
# Use ERROR para menos logs, DEBUG para troubleshooting
MCP_LOG_LEVEL=ERROR

# Log detalhado do FastMCP
FASTMCP_LOG_LEVEL=ERROR

# =============================================================================
# Configurações de Segurança e Limites (Opcional)
# =============================================================================

# Modo somente leitura (impede criação/modificação de recursos)
# READ_OPERATIONS_ONLY=false

# Região padrão para DynamoDB (se diferente de AWS_REGION)
# DYNAMODB_REGION=us-east-1

# =============================================================================
# Como usar este arquivo:
# =============================================================================
# 1. Copie este arquivo: cp .env.example .env
# 2. Edite o arquivo .env com suas credenciais
# 3. Configure o AWS CLI: aws configure --profile default
# 4. Teste a conexão: aws sts get-caller-identity --profile default
# 5. Reinicie o Claude Code para carregar as novas configurações
#
# Para mais informações, consulte: docs/CONFIGURACAO_AWS_MCP.md

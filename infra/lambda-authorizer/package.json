{
  "name": "nfe-lambda-authorizer",
  "version": "1.0.0",
  "description": "Lambda Authorizer para validação JWT via Cognito",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r ../lambda-authorizer.zip ."
  },
  "dependencies": {
    "aws-jwt-verify": "^4.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^20.11.5",
    "typescript": "^5.6.3"
  }
}
